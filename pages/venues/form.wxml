<!--pages/venues/form.wxml-->

<view class="page-container">

    <!-- <form bindsubmit="save">
      <view class="form-title">{{isEdit ? "Edit Venue" : "Add Venue"}}</view> -->

    <view class="section" >
      <input data-name="name" bindinput="updateData" maxlength="15" value="{{venue.name}}" class="weui-input" auto-focus placeholder="Venue Name"/>
    </view>

    <view class="section">
      <input data-name="name" maxlength="15" value="{{venue.name}}" bindinput="updateData" class="weui-input" auto-focus placeholder="Address"/>
    </view>

    <view class="section" >
      <input data-name="phone_number" bindinput="updateData" maxlength="15" value="{{venue.subject}}" class="weui-input" auto-focus placeholder="Phone Number"/>
    </view>

    <!-- <view class="page-section">
      <view class="page-section-name">Category</view>
      <view class="weui-cells weui-cells_after-name">
        <checkbox-group bindchange="checkboxChange">
          <label class="weui-cell weui-check__label" wx:for="{{items}}" wx:key="{{item.value}}">
            <view class="weui-cell__hd">
              <checkbox value="{{item.value}}" checked="{{item.checked}}"/>
            </view>
            <view class="weui-cell__bd">{{item.name}}</view>
          </label>
        </checkbox-group>
      </view>
    </view> -->


      <picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}">
          <view class="search">
                      Category {{array[index]}}
                      <image src="../../images/Category.png" class="category"></image>
              </view></picker>
    
    <view class="section">
    <view>Hours of Operation</view>
      <picker mode="time" value="{{time}}" start="00:00" end="23:59" bindchange="bindStartTimeChange">
        <view class="picker"> Open: 
          <text class="section-picker">{{venue.start_time}}</text>
        </view>
      </picker>

      <picker mode="time" value="{{time}}" start="00:00" end="23:59" bindchange="bindEndTimeChange">
        <view class="picker">
          Close: <text class="section-picker">{{venue.close_time}}</text>
        </view>
      </picker>
    </view>

    <view class="section section-description">
      <textarea data-name="description" value="{{venue.description}}" bindinput="updateData" class="weui-input" auto-focus placeholder="Specials"/>
    </view>


   <!-- <view class="section" >
      <input data-name="phone_number" bindinput="updateData" maxlength="15" value="{{venue.subject}}" class="weui-input" auto-focus placeholder="Phone Number"/>
    </view> -->
   <!-- <view class='demand-col  demand-col2'> -->

    <view class='section'>
      <input data-name="upload_photo" bindinput="chooseImg" maxlength="15" value="{{venue.add}}" class="weui-input" auto-focus placeholder="Upload Photo:"/>
    <!-- </view> -->
      <!-- <text> Upload Photo: </text> -->
    <!-- </view> -->

     <!-- <view class='up-img' bindtap="chooseImg"> -->
       <image src="{{img1}}" catchTap="chooseImageTap" class="section-icon" mode="aspectFit"></image>
     </view>
     </view>
     <!-- </view> -->
     <!-- <view class='up-img' bindtap="chooseImg">
       <image src="{{img2}}" catchTap="chooseImageTap" mode="aspectFit"></image>
     </view>
     <view class='up-img' bindtap="chooseImg">
       <image src="{{img3}}" catchTap="chooseImageTap" mode="aspectFit"></image>
     </view>
     <view class='up-img' bindtap="chooseImg">
       <image src="{{img4}}" catchTap="chooseImageTap" mode="aspectFit"></image>
     </view> -->
   <!-- </view> -->
 <!-- </view> -->

<!-- 
    <view class="section" bindtap="choosePhoto" >
      <input wx:if="{{!lvenue.icon_url}}" class="weui-input"  auto-focus placeholder="Choose Photo"/>
      <image wx:else src="{{venue.icon_url}}"/>
      <view wx:if="{{!venue.icon_url}}" class="section-icon"></view>
    </view>
    <view class="section" height="300rpx"><map height="600rpx"></map></view>

    </form>

    <view class="button-flex">
      <button class="" bindtap="createLesson">Save</button>
      <button class="" bindtap="goBack">Cancel</button>
    </view>




</view>